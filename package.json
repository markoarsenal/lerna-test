{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "publish": "lerna version & lerna publish from-git"
  },
  "devDependencies": {
    "@lerna/publish": "^6.5.1",
    "lerna": "^6.5.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
