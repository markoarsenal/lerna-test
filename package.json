{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "publish": "npx lerna publish --no-private --include-merged-tags"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
